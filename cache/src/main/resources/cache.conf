{
  caffeine {
    # 全局默认配置（可被具体缓存覆盖）
      max-size = 256
      expire-after-write = 2h   # 写入后2小时过期
      expire-after-access = 10m # 访问后10分钟过期
      weak-keys = true          # 使用弱引用Key（可选）
      record-stats = true       # 启用统计（可选）
    }
    redis_standAlone {

    }
  }